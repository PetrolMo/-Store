<!--pages/mine/mine.wxml-->
<!-- <view class="topContainer">
  <view  class="userContainer" wx:if="{{userInfo.openId}}"> 
  <view class="imgBox">
    <image class="image" src="{{userInfo.avatarUrl}}"></image>
  </view>
  <view class="name">
  <view class="nickName">
    昵称：{{userInfo.nickName}}
  </view>
  <view class="isCertificated" wx:if="{{userInfo.isCertificated}}">
    <icon></icon>
    <text>已认证</text>
  </view>
  </view>
  </view>
  <view class="topRightContainer" bindtap="modifyInfo">
    <text>修改个人信息</text>
    <van-icon name="arrow" />
  </view>
</view>
<view class="line"></view> -->

<view class="login-container">
  <view class="title">我的主页</view>
  <view class="login-box">
    <view class="topContainer">
  <view  class="userContainer" wx:if="{{userInfo.openId}}"> 
  <view class="imgBox">
    <image class="image" src="{{userInfo.avatarUrl}}" mode=" aspectFit"></image>
  </view>
  <view class="name">
  <view class="nickName">
    昵称：{{userInfo.nickName}}
  </view>
  <view class="isCertificated" wx:if="{{userInfo.isCertificated}}">
    <icon></icon>
    <text>已认证</text>
  </view>
  </view>
  </view>
  <view class="topRightContainer" hover-class="tapOnHover" hover-stay-time="100" bindtap="modifyInfo">
    <text>修改个人信息</text>
    <van-icon name="arrow" />
  </view>
</view>
  </view>
</view>

